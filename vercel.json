{
  "buildCommand": "prisma generate && DATABASE_URL=file:./prisma/store.db prisma migrate deploy && DATABASE_URL=file:./prisma/store.db npx tsx prisma/seed.ts && npm run build",
  "framework": "nextjs",
  "installCommand": "npm install"
}
